"""Package-shipped templates rendered by CLI scaffolding commands.

Templates live as Python module constants rather than separate `.md`
files so hatchling doesn't need any package-data plumbing — `import`
is the loader.
"""
